# Database

PostgreSQL schema and migrations will live here starting in Phase 2 (SQL Workspace)
and Phase 3 (StreamFlix Dataset).

Planned layout:

```
database/
  migrations/     Schema migrations (e.g. via Alembic)
  seeds/          Seed data scripts per Learning World
  schema.sql      Reference schema dump
```
